
#include <stdio.h>
#include <math.h>
#include <string.h>
#include <stdlib.h>


typedef char * str;

//prototype
str parse_value(str string);
str parse_array(str string);
str parse_object(str string);
